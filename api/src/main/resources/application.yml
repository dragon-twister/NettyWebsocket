server:
  tomcat:
    uri-encoding: UTF-8
  port: 8003
spring:
  profiles:
    active: @env@


management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always

logging:
  level:
    root: info